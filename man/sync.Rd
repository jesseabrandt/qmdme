% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{sync}
\alias{sync}
\title{Generate or update Quarto code companions for source files}
\usage{
sync(
  paths = NULL,
  extensions = NULL,
  root = ".",
  warn_no_site = getOption("qmdme.warn_no_site", TRUE)
)
}
\arguments{
\item{paths}{Character vector of paths under \code{root} to narrow scope, or
\code{NULL} (default) to walk the whole project. Each path is recursed.}

\item{extensions}{Named character vector mapping file extension to
Quarto chunk-language tag, or \code{NULL} (default) to use the package
default \code{c(R = "r", sql = "sql", py = "python")}.}

\item{root}{Project root. Defaults to the current directory.}

\item{warn_no_site}{Warn when the companions aren't connected to a site
(no \verb{_quarto.yml} references \verb{qmd/}, or there is no site at all)? Defaults
to the \code{qmdme.warn_no_site} option, or \code{TRUE}. Set
\code{options(qmdme.warn_no_site = FALSE)} to silence it for good (e.g. when you
deliberately don't want a site), or pass \code{FALSE} for a single call.}
}
\value{
Invisibly, a data frame with one row per source file with
columns \code{source}, \code{target}, \code{action} (\code{"created"}, \code{"updated"}, or
\code{"recovered"}).
}
\description{
For each source file matching \code{extensions}, writes or updates a
corresponding \verb{qmd/<mirror>/<name>.qmd} file. Existing companion files
have their auto-generated code section refreshed; user-authored prose
above the sentinel is preserved. If a companion file is missing its
sentinel (e.g. it was edited away), a fresh code section is appended
to the bottom and \code{sync()} issues a warning so the user notices.
}
